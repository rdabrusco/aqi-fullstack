{
  "name": "aqi-checker-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build-frontend": "cd ./frontend  && npm run build",
    "start-backend": "cd ./backend && npm start",
    "start": "npm run build-frontend && npm run start-backend -- --loglevel=verbose"
    
  },
  "dependencies": {}
}

